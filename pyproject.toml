[tool.poetry]
name = "seattle-energy-efficiency"
version = "0.1.0"
authors = ["mcourte"]

[tool.poetry.dependencies]
python = "^3.12"
bentoml = "^1.2.0"
scikit-learn = "1.7.2"
pandas = "^2.0.0"
pydantic = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"